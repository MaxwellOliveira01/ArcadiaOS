# ArcadiaOS


## Como rodar:

Basta compilar os códigos e rodar, todos os comandos necessários estão configurados via Make, veja abaixo.

### Make
    make -> compila o codigo
    make run -> roda o codigo compilado com os arquivos de /testcases
    make clean -> apaga os arquivos gerados
